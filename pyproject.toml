[project]
name = "flight-monitor"
version = "0.1.0"
description = "Monitoramento de preços de passagens aéreas com alertas via Telegram"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flights>=0.9.0",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
